query Projects {
    projets(first: 100) {
        nodes {
            title
            slug
            featuredImage {
                node {
                    sourceUrl(size: MEDIUM)
                }
            }
            categories {
                nodes {
                categoryId,
                slug,
                name
                }
            }
        }
    }
}