{
  posts(first: 100) {
    edges {
      node {
        id
        title
        slug
        featuredImage {
          node {
            sourceUrl(size: LARGE)
          }
        }
      }
    }
  }
}
